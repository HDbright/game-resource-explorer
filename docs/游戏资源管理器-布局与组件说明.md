# 游戏资源管理器 — 全面布局与组件说明

> 对象：游戏资源管理器（Electron 桌面应用，v1.9.29）
> 定位：管理并预览 **动画(Spine/DragonBones) / 图片 / 音频 / 3D(model)** 四类游戏资源；内置资源工具箱（FGUI 逆向导出、FGUI 编辑器、ASTC→PNG、SKEL→JSON、Spine 修复、图片编辑）；游戏场景管理（含 FGUI 界面预览器）；**网络资源抓取**（内嵌浏览器逆向分析并下载 FGUI/Spine/图集/图片/音频 等资源入库，支持多标签页 / 网页悬浮窗 / 资源悬浮预览窗）；**开发工具箱 → API 管理**（项目 / API 数据字典 / 接口测试 + 内嵌 API 参考文档）；网址收藏夹；系统设置。
> 技术栈：Electron + Vite + 原生 JS(ESM)；渲染 PixiJS 8（动画/图片/FGUI 画布，经 `src/pixiLazy.js` 动态导入）、DOM overlay（文本）、WebContentsView（内嵌网页）；独立 BrowserWindow（网页悬浮窗 / 资源悬浮预览窗）；数据持久化 better-sqlite3。

---

## 一、应用架构与数据流

```
Electron 主进程(electron/main.js + electron/tools/*)
   ├─ 窗口/菜单/文件对话框(shell, dialog)
   ├─ IPC 通道(db:read/write, fs:*, tool:*, fgui:*, audio:*, thumb:*, web:* …)
   ├─ 资源扫描(electron/scanner.js: Spine/DB/图片/音频/3D/FGUI 识别)
   ├─ 转换工具(ASTC→PNG、SKEL→JSON、Spine 修复)
   ├─ FGUI 逆向(fgui/index.js: 探测/解析/导出/源工程还原) + 预览数据(previewData.js)
   ├─ 网络资源抓取(tools/webGame.js: WebContentsView 多标签 + 请求拦截 classify + 下载入库 + 网页悬浮窗 floatWin)
   ├─ 资源预览浮窗(webPreviewWindow.js: 独立 BrowserWindow 悬浮预览; tools/apiTest.js: API 接口测试代理)
   └─ SQLite 库(data/skeleton.db: items/categories/fav_*/scenes/web_bookmark_* 等)

渲染进程(src/**: main.js 入口 → state.js 状态 → ui.js 布局与导航 → pages/*.js 各页面 → viewers/*.js 查看器)
   └─ index.html: 单一 #app → 顶栏 + 侧栏 + #content-panel(10 个 page 容器，含 #page-api)
```

数据流：`state.js`(内存) ⇄ `window.api.dbRead/dbWrite`(IPC) ⇄ SQLite；所有增删改经 `saveState()` 防抖 150ms 写盘。
网络抓取数据流：`tools/webGame.js` 拦截 WebContentsView 网络请求 → `classify` 分类 → `onWebCaptured` 推送渲染进程列表 → 选中/批量 `downloadResource`(主进程 https 下载) → `addScene`(subtype='fgui') / `addItem` 入库。

---

## 二、全局框架布局

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 顶栏 #app-header                                                             │
│  ☰(侧栏开关 #btn-toggle-side) 品牌名(点击回首页) 面包屑 #breadcrumb          │
│  🔍搜索 #search(#search-clear)  +添加资源 #btn-add  +批量 #btn-add-batch       │
│  ⚙设置 #btn-settings   ┃ 资源标签 #resource-tabs(动画|图片|音频|3D)           │
├───────────────┬──────────────────────────────────────────────────────────────┤
│ 侧栏 #sidebar  │ 主内容区 #content-panel(十页之一)                           │
│  #cat-tree     │  page-home(资源首页) / page-audio-home / page-folder        │
│  分类树:       │  page-preview(预览) / page-toolbox(工具箱)                  │
│  ·动画/图片/…  │  page-scene(场景管理) / page-fgui-editor(FGUI编辑器)        │
│  ·收藏夹       │  page-webgame(网络资源抓取) / page-settings(设置)           │
│  ·资源工具箱   │                                                             │
│  ·游戏场景管理 │                                                             │
│  ·网络资源抓取 │                                                             │
└───────────────┴──────────────────────────────────────────────────────────────┘
  底部/悬浮: 音频迷你条 #audio-mini(后台播放控制) · 通知 toast · 右键菜单浮层
```

### 2.1 顶栏
| id | 控件 | 功能 |
|---|---|---|
| `#btn-toggle-side` | 侧栏开关 | 显示/隐藏侧栏（localStorage 记忆） |
| 品牌名 | 首页入口 | 点击回资源首页（`resourceTab='home'`，清空一切覆盖页状态） |
| `#breadcrumb` | 面包屑 | 当前页路径（如 资源首页 › 动画 › 某分类） |
| `#search` | 全局搜索框 | 在目录页过滤资源（名称/属性/标签）；`#search-clear` 清空 |
| `#btn-add` | + 添加资源 | 打开添加流程（文件/目录扫描识别资源类型） |
| `#btn-add-batch` | + 批量添加 | 批量导入 |
| `#btn-settings` | ⚙ 设置 | 打开系统设置页 |
| `#resource-tabs` | 资源标签 | 动画/图片/音频/3D 四个类型 tab；当前 tab 高亮，主区切到类型主页/目录 |

### 2.2 侧栏分类树 `#cat-tree`
- **类型根节点**（id=`all`，名称随 tab：动画资源/图片资源/…/资源）：类型主页链接 + 展开/折叠该类型分类目录（默认展开）
- **分类节点**：递归展开子分类/直属条目；条目单击 → 预览（FGUI 包直达预览）；右键菜单（打开/新建目录/编辑/删除/移动/重命名…）
- **未分类节点**：无分类条目
- **收藏夹根** `__fav__`：收藏分类树
- **资源工具箱根** `__tools__`：文件格式转换（astc→png / skel→json / spine 修复）、图片编辑、**FGUI导出**（逆向导出）、**FGUI编辑器**（独立可视化编辑器入口 `__fgui_editor__`）
- **游戏场景管理根** `__scene__`：场景分类树 + FGUI 界面预览入口；FGUI 包(.bin)条目点击/右键「✏️ 用FGUI编辑器打开」
- **网络资源抓取根** `__webgame__`：内嵌浏览器逆向抓取资源入口；下含「🕹 最近打开」(`webGameHistory` 子节点，点击直达回填 URL) 与「🔖 网址收藏夹」分类树(`__webgame_fav__`，可嵌套子目录，默认展开，条目可增删改查)
- 树节点构造器 `makeTreeNode`：▶/▼ 箭头 + 图标 + 名称 + 数量
- 网络抓取根节点右键：打开抓取页 / 新建收藏夹目录 / 清空捕获记录；网址收藏夹分类节点右键：打开收藏夹 / 新建子目录 / 编辑目录 / 删除目录
- **开发工具箱根** `__devtools__`（🛠️，默认展开可折叠）：下含「📖 API 管理」叶子节点 `__devtool:api`（点击进入 `#page-api` 的 API 管理页）；由 `renderDevToolsSection` 渲染，`enterApiDoc()` 进入（详见 **十六、API 管理模块**）

### 2.3 主内容区页面容器
`page-home` / `page-audio-home` / `page-folder` / `page-preview` / `page-toolbox` / `page-scene` / `page-fgui-editor` / `page-webgame` / `page-settings` / `page-api`（`showPage()` 切换，共 **十页**）。其中 `page-fgui-editor` / `page-webgame` / `page-api` 为新增独立页：
- `page-fgui-editor`：FGUI 编辑器（IDE 式布局，复用 FguiLayoutPreview 画布引擎，详 **十四、FGUI 编辑器模块**）
- `page-webgame`：网络资源抓取（内嵌 WebContentsView 叠加于该页容器之上，详 **十五、网络资源抓取模块**）
- `page-api`：开发工具箱 → API 管理（双标签页：项目管理 + API 文档 iframe，详 **十六、API 管理模块**）

### 2.4 主区多标签页（v1.6.9+）
- 主内容区顶部新增标签条 `#tab-strip`（`.main-tab` 项：图标 + 名称 + **悬停显示的关闭符号 ×**）
- **打开资源 = 新标签页**（资源名 + 类型图标 🎬🖼♪🧊）；点击类型/目录/工具箱/场景/设置/首页等导航 = 切换到对应标签页，原标签保留可随时切回
- 关闭当前标签自动切到相邻标签；全部关闭回首页
- 标签同步：`renderMainArea` 渲染前 `syncTabFromState()` 自动对齐；`ensureTab/switchTab/closeTab/applyTabState` 管理生命周期；修复 FGUI 预览时点「全部资源首页」无法切换的问题

---

## 三、资源首页（page-home / global home）

| 模块 | 内容 |
|---|---|
| 标题区 | 「游戏资源管理」+ 资源总数统计 |
| 统计卡片 `#home-cards` | 动画/图片/音频/3D/总数 × 各类型占用空间；点击切到类型主页 |
| 目录快捷入口 `#home-quick-cats` | 顶级分类卡片（名称+条目数+占用），点击进入目录列表页 |
| **最近打开 `#home-recent-opens`**（v1.6.7+） | 最近打开的**资源 / FGUI 包**记录：类型徽标 + 名称 + **打开日期时间**（YYYY-MM-DD HH:mm）；点击**再次打开**（资源→预览；FGUI→FGUI 预览）；数据 `settings.recentOpens`（去重按路径、最新在前、上限 20） |
| 最近添加 `#home-recent-list` | 最近入库资源（类型徽标+名称+分类），点击预览，右键菜单 |

> 类型主页（tab=anim/image/audio/3d 且目录=全部）：该类型统计卡片 + 层级分类目录树 + 最近添加。音频类型主页 = 音频播放器主页（`page-audio-home`）。

### 3.5 目录管理模式（v1.9.15+，仅资源首页 `page-home`）
全局主页标题行右侧新增「🛠 管理」按钮（`#home-mgmt-btn`，管理时变为「✓ 完成管理」；代码 `src/pages/homePage.js` 模块级状态 `homeManage`，由 `renderMainArea` 主页分支传入 `onManageDelete`/`onManageMove`）。
- 进入管理模式后：目录快捷入口每项前显示勾选框（`.qc-check`，`data-check=<分类id>`）；标题行出现「☑ 全选」（`#home-mgmt-all`）+ 操作条（已选计数 `#home-mgmt-count` / 📂 移动 `#home-mgmt-move` / 🗑 删除 `#home-mgmt-del`，未勾选时禁用）；点击目录不跳转（`homeManage.active` 时 return）。
- **批量删除**（`batchDeleteCategories`，`ui.js`）：默认语义「删除目录下的所有动画和子目录（仅从列表移除，不删磁盘文件）」，`removeCategoryAdvanced({deleteItems:true, subAction:'parent'})`，确认框统计目录/子目录/动画数。
- **批量移动**（`batchMoveCategoriesDialog`）：目标排除选中目录及其子孙，支持移至顶级。
- 样式：`.home-title-row` / `.home-mgmt-bar` / `.home-mgmt-selectall` / `.quick-cat.mgmt` / `.qc-check`（`src/style.css`）。
- 同时 `deleteCategoryDialog`（普通右键删除目录）默认项改为第一项「删除所有动画和子目录」（`rbDel.checked = true`，子目录区块联动隐藏）。

---

## 四、目录列表页（page-folder）

| 区 | 控件 | 功能 |
|---|---|---|
| 工具栏 | 返回/分类名/条目数 | 面包屑式导航 |
| | 视图模式 | 详情(表格)/列表/图标 三态切换 |
| | 排序 | 名称/类型/大小/日期 × 升/降序 |
| | 标签过滤 + 搜索框 | 按标签过滤（`#folder-tag-filter`）、名称/属性/标签搜索 |
| | 编辑模式开关 | 批量操作模式（多选/全选/删除/移动/改标签） |
| 资源列表 | 详情/列表/图标视图 | 缩略图：动画=`thumbnailService` 异步生成、图片=`/a/<id>/`、音频♪、3D🧊 |
| | 条目右键菜单 | 预览/打开目录/编辑/删除/标签/移动… |
| 分类子卡片 | 子分类卡 | 点击进入子分类，右键菜单 |

---

## 五、预览页（page-preview）

顶部条：返回 `#pv-back` / 类型 `#pv-type` / 名称 `#pv-name` / 版本 / 路径 `#pv-path` / 打开目录 `#pv-open-dir` / 刷新 `#pv-reload`；下方三子视图按类型切换（`#pv-anim-view` / `#pv-image-view` / `#pv-audio-view` / `#pv-fgui-view`(旧版 FGUI)）。

### 5.1 动画预览（Spine/DragonBones，PixiJS）
- 控制条：播放/暂停 `#btn-play`、重启 `#btn-restart`、速度 `#speed-range`、缩放 `#zoom-range`+模式(`fit/100%/fixed`) `#zoom-mode`、帧导航 `#frame-*`、动画切换 `#anim-select`、背景色 `#bg-*`、显示骨骼 `#show-bones`、水平翻转 `#flip-x`、旋转 `#btn-rotate`、截图 `#pv-capture`
- 运行时路由：3.x → Spine38Player(vendor/spine38)；4.x → SpinePlayer(@pixi/spine-pixi)；DragonBones → vendor/dragonbones

### 5.2 图片查看
缩放 `#img-zoom-range`、适配/原始大小、背景色、状态栏

### 5.3 音频播放器（AudioPlayerController）
单曲/目录/播放列表队列，6 模式，变速 `#audio-rate`，ID3 编辑，迷你条 `#audio-mini`（离开预览后台播放）

### 5.4 3D 模型
占位视图（showModelPlaceholder，无内置渲染器）

---

## 六、资源工具箱（page-toolbox）

| 工具 | 说明 |
|---|---|
| ASTC → PNG | 读取 `.astc`（16 字节头），软件解码转 PNG |
| SKEL → JSON | Spine 二进制骨架转 JSON（3.x/4.x 两套解析） |
| Spine 修复 | 剥 BOM/注释/尾逗号/补版本、skel 探测、atlas 检查 |
| 图片编辑 | 内置图片编辑工具 |
| FGUI导出 | FGUI .bin 逆向导出源工程（FGUI_src/<包名>/：组件 XML + 碎图 + 字体 + 动画 + 声音） |
| FGUI编辑器 | 独立可视化编辑器（见 **十四、FGUI 编辑器模块**）；菜单位于资源工具箱下的「FGUI编辑器」入口 |

---

## 七、游戏场景管理（page-scene）

### 7.1 场景主页
统计卡片（场景总数/文件夹/文件/目录数）+ 操作（+添加场景 / 🧩添加FGUI包 / +新建目录）+ **FGUI 界面预览入口卡** + 场景分类树 + 最近添加（右键菜单：FGUI 预览/显示/打开/编辑/删除）

### 7.2 场景目录页
工具栏（返回/标题/数量/+添加场景/🧩添加FGUI包/+新建子目录）+ 子分类卡 + 场景表格（图标/名称/路径/大小/操作：🧩FGUI预览、显示、打开、编辑、移动、删除）；**FGUI 包行点击直接打开预览**；size 缺失惰性补全

### 7.3 FGUI 界面预览子页（重点，见《docs/fgui-previewer-layout.md》）
- 三区：工具栏（14 控件）/ 画布（PixiJS + 文本 DOM）/ 右侧面板（组件列表 + 分割线 + 快照条 + 属性面板，可拖拽布局）
- 登记/解压/导出/快照/撤销历史/控制器切换/组件定位高亮

---

## 八、系统设置（page-settings）
播放器默认（模式/倍速/背景/缩放）、截图（目录/格式/入库）、音频播放器（模式/播放列表显示字段）、资源库（库文件位置等）、其他偏好

---

## 九、通用组件（src/dialogs.js）
| 组件 | 说明 |
|---|---|
| `openModal` | 模态框容器（标题+body+footer） |
| `promptDialog` | 表单对话框（text/select/checkbox/textarea 字段） |
| `confirmDialog` | 确认框（支持 `onCancel` 回调） |
| `toast` | 轻提示（success/error/info，自动消失） |
| `showContextMenu` | 右键菜单浮层（点击外部关闭） |
| `footButtons` | 底部按钮组构造 |

---

## 十、快捷键
| 键 | 功能 |
|---|---|
| Ctrl+Z | FGUI 预览编辑模式撤销 |
| 空格 | 动画播放/暂停（预览页） |
| ← / → | 动画帧上/下（预览页） |
| Ctrl+F | 聚焦搜索（如已实现） |

---

## 十一、数据持久化（data/skeleton.db）
- 表：`items`(资源)、`categories`(分类)、`fav_categories`/`fav_items`(收藏夹)、`scene_categories`/`scenes`(场景；`subtype`/`fgui_snapshots` 标记 FGUI)、`settings`(JSON)、`web_bookmark_categories`/`web_bookmarks`(网址收藏夹)
- `settings` 关键项：resourceTab、viewMode/sortBy/sortDir、audioMode/audioRate/audioPlaylists、**recentOpens(最近打开)**、screenshot*、zoomMode/bgColor、**webGameLastUrl / webGameSaveDir / webGameProxy / webGameHistory**(网络资源抓取持久化)
- 旧库迁移：缺列自动 ALTER TABLE（size/mtime/tags/typeTags/subtype/fgui_snapshots）；收藏夹表为新建表，随库升级创建

---

## 十二、代码结构（src/）
| 文件 | 职责 |
|---|---|
| `main.js` | 应用入口：加载 state、初始化 UI、IPC 监听 |
| `state.js` | 状态模型 + CRUD + `recordRecentOpen`/`findSceneByFilePath` 等 |
| `ui.js` | 布局/导航/侧栏树/顶栏/事件中枢（~2800 行） |
| `dialogs.js` | 通用对话框/菜单/通知 |
| `addFlow.js` | 添加资源流程（文件/目录扫描） |
| `thumbnails.js` | 动画缩略图服务 |
| `clipboard.js` | 复制粘贴 |
| `pages/homePage.js` | 首页（统计/最近打开/最近添加/目录快捷） |
| `pages/folderPage.js` | 目录列表页 |
| `pages/toolboxPage.js` | 资源工具箱 |
| `pages/scenePage.js` | 场景管理 + FGUI 预览子页 |
| `pages/fguiEditorPage.js` | **FGUI 编辑器页**（IDE 布局：顶栏/左资源面板/画布/右属性，详 十四） |
| `pages/webGamePage.js` | **网络资源抓取页**（内嵌浏览器 + 资源捕获 + 网址收藏夹，详 十五） |
| `pages/settingsPage.js` | 设置页 |
| `viewers/fguiLayoutPreview.js` | FGUI 预览/编辑器共用 PixiJS 渲染与交互引擎 |
| `viewers/*` | 动画/图片/音频/FGUI 查看器 |
| `electron/tools/webGame.js` | 网络抓取 WebContentsView 多标签：open/setBounds/close + 请求拦截 classify + 下载 downloadResource + 本地探测 probeFile + 网页悬浮窗 floatWin(floatOut/floatBack/floatMinimize/floatClose) + openOrSwitch + pauseMedia + fetchToDataUrl |
| `electron/tools/webPreviewWindow.js` | 资源悬浮预览独立 BrowserWindow（show/hide/togglePin/持久化位置大小，partition `persist:webgame`） |
| `electron/tools/apiTest.js` | API 接口测试代理（Node http/https，rejectUnauthorized:false + 可选代理 + 重定向≤5） |
| `electron/floatPreload.js` | 网页悬浮窗专用预加载脚本 |
| `src/pages/apiPage.js` | **API 管理页**（双标签页：项目管理 / API 文档，三级模型 CRUD + 接口测试面板） |
| `src/pixiLazy.js` | PixiJS 动态导入（`getPixi`/`pixiRef`），首屏不加载 918KB pixi，首次预览/缩略图/FGUI 编辑时才加载 |
| `electron/server.js` | 本地 HTTP 服务（**固定端口 13456**，修复 localStorage 持久化失效） |
| `public/preview-window.html` + `electron/previewPreload.js` | 资源悬浮预览窗页面与预加载（图片/音频/视频/字体/文本 + FGUI/Spine 入库按钮） |
| `public/float-window.html` | 网页悬浮窗页面（自绘标题栏：🌐 标题 + ─ 最小化 + ✕ 关闭） |
| `public/api-doc.html` | API 参考文档（由 `E:\MyProject\api_page\index.html` 移植，iframe 内嵌于 API 管理页） |
| `electron/tools/fgui/*` | FGUI 探测/解析/导出源工程/预览数据 |
| `electron/*` | 主进程、scanner、tools(fgui/skel/astc…) |
| `index.html` / `style.css` | 全局骨架与样式（含 .fge-* / .wg-* 样式段） |

---

## 十三、布局演进记录
| 版本 | 变更 |
|---|---|
| v1.6.4 | FGUI 预览器组件列表面板（树形 + @外部包名 + 点击高亮） |
| v1.6.5 | FGUI 预览器右侧可拖拽布局（50/50 分割线 + 面板拖宽 + 尺寸记忆） |
| v1.6.6 | 组件列表完整化（本包节点不缺失）+ 点击联动属性面板 + 下拉双向同步 |
| v1.6.7 | **首页「最近打开」模块**（时间展示 + 点击再次打开，覆盖资源/FGUI/场景路径） |
| v1.6.8 | **FGUI 预览画布背景色**可更改（`fguiBgColor` 持久化） |
| v1.6.9 | **背景色三模块统一调色盘**；**类型主页最近打开**（按类型过滤）；**主区多标签页**（打开资源新标签 / 切换 / hover 关闭） |
| v1.7.0 | **顶栏搜索与侧栏上下文联动** + **FGUI 组件列表搜索**；九页容器框架就绪 |
| v1.7.7 | FGUI 预览新增**位图字体渲染**支持（title/按钮/战力数字接近 FairyGUI 编辑器效果） |
| v1.7.8 | 位图字体补充 **.fnt 文本文件兜底**路径 |
| v1.7.9 | 位图字体**导出源工程字形 PNG 兜底**（图集缺失时直接加载独立 PNG 渲染） |
| v1.7.11 | FGUI 预览 PNG **预乘 alpha 修复**（常量 `premultiply-alpha-on-upload`，透明边缘不再发白） |
| v1.7.12 | FGUI 预览工具栏移除「解压FGUI包」，保留「导出源工程」；导出目录调整为 `FGUI_src/<包名>/` |
| v1.7.13 | **新增 FGUI 编辑器模块**（独立可视化编辑器，IDE 式布局，见 十四） |
| v1.7.14 | FGUI 编辑器**入口整合**（菜单移入资源工具箱）+ **状态保持**（切页/切标签保留打开包与编辑状态） |
| v1.7.15 | FGUI 编辑器四大增强：组件列表/层级树分割线可拖动、层级树折叠展开、资源预览+主区 9 点高亮+属性关联、源工程编辑与保存 |
| v1.7.16 | FGUI 编辑器属性面板**名称/id 可编辑** + 字段（srcPkg/sprite/atlas）一键复制 |
| v1.7.17 | **新增网络资源抓取模块**（内嵌浏览器逆向分析并下载 FGUI/Spine/图集/图片/音频 入库，见 十五） |
| v1.8.0 | 主版本号升级（内容同 v1.7.17） |
| v1.8.1 | 网络资源抓取 **DevTools 独立窗口入口** + **网址收藏夹增强**；侧栏节点/面包屑改名「网络资源抓取」（原「网页游戏抓取」） |
| v1.8.2 | 网络抓取顶栏 **网页音频静音** 🔊/🔇（`#wg-mute` + `web:setAudioMuted`） + **隐藏侧栏** 🗂/👁（`#wg-toggle-side`） + 筛选行整合（`#wg-chips` 容器） |
| v1.8.3 | 隐藏侧栏改为**折叠高度**（浏览器区 `flex:1` 占满）；静音/隐藏侧栏按钮改纯图标 |
| v1.8.4 | DevTools 按钮改纯 `</>` 符号（`#wg-devtools`） |
| v1.8.5 | 状态文本 `#wg-status` 移入地址栏尾部（`.wg-url-wrap` 内绝对定位）；DevTools 按钮移到「🔖 收藏」之后 |
| v1.8.6 | 捕获列表 **勾选框 + 缩略图 + 右键保存/预览 + 悬浮预览窗**（DOM `.wg-pv`，后于 v1.9.0 改为独立窗口） |
| v1.8.7 | 预览窗 **图钉常驻** + 不被浏览器区遮挡（`syncBrowserOcclusion`，方案后废弃） |
| v1.8.8 | 侧栏「XX资源」根**默认折叠**（`expandedCats` 不再含 `'all'`）；预览窗音频播放修复（带 referrer 下载 → data URL） |
| v1.8.9 | 预览窗与浏览器区重叠时**截图快照垫底**（方案后废弃） |
| v1.9.0 | **悬浮预览窗改为独立 BrowserWindow**（`webPreviewWindow.js` + `preview-window.html`），彻底解决 DOM 盖不住原生 WebContentsView |
| v1.9.1 | 独立预览窗**不再随鼠标移动**（记住 `lastPos`，再次显示恢复原位） |
| v1.9.2 | 捕获列表缩略图兜底（`webThumbFetch`→`fetchToDataUrl` 共享 cookie/Referer）+ **悬浮预览开关** `#wg-pv-switch`（`wg-pv-enabled`） |
| v1.9.3 | 捕获列表**文件名搜索** `#wg-search`（与类型 chips / 仅下载不入库叠加过滤） |
| v1.9.4 | **资源归类修正** `fixRecordTypes`：`.bin/.fui→fgui`；spine 同名配套（`.skel/.atlas/.png/.astc`）整组归 `spine` |
| v1.9.5 | 新增**「开发工具箱」根** `__devtools__` + **API 管理页** `#page-api`（iframe 内嵌 `api-doc.html`） |
| v1.9.6 | **API 管理模块**：项目管理（分类树/项目/API 数据字典三级）+ **接口测试**（`apiTest.js`，三表 SQLite） |
| v1.9.7 | 网络抓取 **多标签页浏览** `#wg-tabs`（`WebGameView.tabs` Map + `newTab/switchTab/closeTab/getUrl`）+ 收藏夹增强（新标签打开/移动到/未分类重命名） |
| v1.9.8 | 收藏夹点击**新标签打开** + **移动到分类** + 未分类可重命名 + `refreshTree` 同步左侧树 |
| v1.9.9 | 取消「未分类」+ **收藏必须选分类**（无分类则输入新建）；面板新增「全部」虚拟视图 |
| v1.9.10 | `.sk` 文件归类 `spine`（`EXT_TYPE` 新增 `[/\.sk(\?|$)/i,'spine']`） |
| v1.9.11 | 捕获列表**「打开目录」按钮** `#wg-open-dir` + 「下载选中」去高亮 |
| v1.9.12 | spine **预览图**（同组首个 png）+ **配套整组保存** + **按域名/URL 路径归档**（去 gameName/type 层） |
| v1.9.13 | 捕获列表右键**「打开下载目录」**（已下载项显示，跳 `openPath`） |
| v1.9.14 | **网页悬浮窗** `floatWin`（可拖拽/最小化/关闭，切模块时网页视图迁出主窗口；`float-window.html` + `floatPreload.js`） |
| v1.9.15 | 资源首页**目录管理模式**（🛠 管理 + 批量勾选/删除/移动） |
| v1.9.16 | **启动提速**：`pixiLazy.js` 动态导入 + `localStorage` 延迟读取 + `preview.init` 延迟（首屏 gap ~4.9s→4ms） |
| v1.9.17 | **启动骨架屏** `#splash`（内联 CSS，窗口 `show:false` + `ready-to-show` 后显示，消除黑屏） |
| v1.9.18 | 关闭窗口**彻底退出**（`win.on('closed')` 清理附属窗 + `app.quit()`）+ handler 注册顺序修复 |
| v1.9.19 | 悬浮预览窗默认位置改**鼠标右下方**（不遮挡缩略图/文件名） |
| v1.9.20 | **运行状态持久化**：`wg-only-url`/`wg-filter-types`/`wg-search` + 音频 `audio-mode`/`audio-rate`/`audio-volume` |
| v1.9.21 | 悬浮窗**位置/大小持久化**（重启恢复，`userData/*.json` 节流保存） |
| v1.9.22 | 资源行**播放按钮 ▶**（悬停显示，点击在悬浮预览窗播放，`showPreview` 绕过开关） |
| v1.9.23 | **固定端口 13456** 修复 localStorage 持久化失效（端口随机→origin 变→全丢）；悬浮窗迷你按钮（64×36） |
| v1.9.24 | 悬浮窗**关闭真正关闭/最小化迷你按钮**（64×32）+ `shownRecords` 始终按类型筛选（修复筛选失效） |
| v1.9.25 | 悬浮窗关闭后残留修复 + 切走页面**暂停网页媒体** `pauseMedia` |
| v1.9.26 | 侧栏收藏夹点击**不再左上角小窗** + `openOrSwitch`（已开相同 URL 切换标签，否则新开） |
| v1.9.27 | 有网页时点击收藏夹目录**保持内容不黑屏**（`enterWebGame` 跳过 `clearOverlays` + `keepBrowser`） |
| v1.9.28 | 捕获列表右键**「另存..」** `saveAsRec`（目录选择器，保留原输出目录） |
| v1.9.29 | 「另存」默认定位**输出目录** + 捕获列表播放按钮改**悬浮线条风格**（`.wg-thumbwrap` 28×28 插槽 + `.wg-playbtn` 内联 SVG） |

---

## 十四、FGUI 编辑器模块（page-fgui-editor）

> 代码：`src/pages/fguiEditorPage.js`（页面/工具栏/事件）、`src/viewers/fguiLayoutPreview.js`（PixiJS 渲染与交互，与预览器共用）、`src/style.css`（`.fge-*` 样式）
> 入口：左侧「资源工具箱」→「FGUI编辑器」菜单（`enterFguiEditor`）；场景管理中 FGUI 包(.bin)条目点击/右键「✏️ 用FGUI编辑器打开」→`openFguiEditorFromScene`；最近打开记录 FGUI 项点击。
> 定位：**独立于 FGUI 界面预览器**的可视化编辑器，布局参考 FairyGUI-Editor-Online 的 IDE 结构（顶栏 + 左资源面板 + 中画布 + 右属性）。复用 FguiLayoutPreview 画布引擎与 `fguiPreviewLoad` 数据层。

### 14.1 整体布局（IDE 四区）
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ ① 顶栏 .fge-toolbar                                                         │
│  📦选择FGUI包 | 包名(v版本) | 组件下拉 ▼ | …… | ✎编辑模式 | ↩撤销          │
│  | 💾保存源工程 | 📤导出源工程 | 📂打开目录 | 背景[color] | 状态文本         │
├───────────────┬────────────────────────────────────────┬───────────────────┤
│ ② 左资源面板   │ ③ 画布区 .fge-canvas-wrap(flex:1)       │ ④ 右侧面板 .fge-  │
│   .fge-left    │   <canvas id="fge-canvas">             │   right           │
│   [🧩组件树 │   .fg-text-layer 文本 DOM 叠加            │  ┌ 控制器          │
│    🗂资源]    │   交互：滚轮缩放/拖拽/点选/编辑模式      │  ├ 属性            │
│   组件列表     │                                        │  └ (点击对象显示) │
│   ──分割线──   │                                        │                   │
│   层级树(可折叠)│                                       │                   │
│  ──资源 tab──  │                                        │                   │
│   图片/字体/   │                                        │                   │
│   动画/声音 +  │                                        │                   │
│   资源预览     │                                        │                   │
└───────────────┴────────────────────────────────────────┴───────────────────┘
```

### 14.2 顶栏控件（①区）
| id | 名称 | 功能 | 默认 |
|---|---|---|---|
| `#fge-pick` | 📦 选择 FGUI 包(.bin) | 弹出文件选择加载 .bin | 常显 |
| `#fge-pkg` | 包名（文本） | 显示 `包名 (v版本)` | 加载后填充 |
| `#fge-comp` | 组件下拉 | 选择要编辑的组件，切换即 `loadComp` 重渲染 | 加载后启用 |
| `#fge-edit` | ✎ 编辑模式 | 开关可视化编辑（拖拽移动/8 手柄缩放/属性面板可编辑/双击文本内联编辑） | 加载后启用 |
| `#fge-undo` | ↩ 撤销 | 撤销上一步编辑（Ctrl+Z 同效） | 编辑模式启用 |
| `#fge-save-src` | 💾 保存源工程 | 把当前组件编辑结果写回 `FGUI_src/<包名>/<组件>.xml`（源工程不存在自动先导出） | 加载后启用 |
| `#fge-export` | 📤 导出源工程 | 导出完整 FairyGUI 源工程到 bin 同目录 `FGUI_src/<包名>` | 加载后启用 |
| `#fge-opendir` | 📂 打开目录 | 资源管理器打开当前包所在目录 | 加载后启用 |
| `#fge-bg` | 背景[color] | 画布背景色（立即生效 `setBackground`） | 常显 |

### 14.3 左资源面板（②区）
两个 tab（`.fge-tab`）：
- **🧩 组件树**：上半「组件列表」(`#fge-complist`，列出包内全部组件 `名称 (宽×高)`) + 中间 `#fge-hsplit` 分割线（可上下拖动调组件列表/层级树高度比） + 下半「层级树」(`#fge-hier`，按深度缩进列出当前组件所有节点，箭头可折叠/展开，点击与画布选中联动，折叠状态提示 `N 处折叠`)。
- **🗂 资源**：四个子 tab（`.fge-rtab`）：🖼 图片 / 🔤 字体 / 🎞 动画 / 🔊 声音；列表 `#fge-reslist` 显示该类型全部资源（路径/尺寸）；点击资源 → 主区 `#fge-canvas` 中**使用该资源的节点 9 点高亮**(`highlightResource`) + 右侧属性面板显示资源属性 + 下方「资源预览」区(`#fge-res-pv-body`)：图片按 sprite 裁剪渲染（rotated 逆时针转回）、字体/动画展示信息、声音提供「▶ 播放」按钮。

### 14.4 画布区（③区）与右面板（④区）
- 画布：复用 FguiLayoutPreview（PixiJS 独立 App，`ticker.stop()` + 手动 render），`#fge-text` 文本 DOM 叠加层（天然支持中文/换行/选中框）。支持滚轮缩放、拖拽平移、点选高亮、编辑模式拖拽/缩放/双击文本内联编辑、Ctrl+Z 撤销。
- 右侧面板：顶部「控制器」(`#fge-ctrls`，每个控制器一组页名按钮，切换控制器页并按 gearDisplay 重算可见性) + 下方「属性」(`#fge-props`，点击画布/层级树对象后显示；v1.7.16 起名称/id 可编辑、srcPkg/sprite/atlas 字段可一键复制)。

### 14.5 状态保持与联动
- 页面首次进入初始化一次（`container._fguiEditorInited` 标记），切到其它菜单/标签页再回来时**保留已打开的包、组件与编辑状态**，不重建画布；外部再次加载走 `container._fguiEditorLoad(binPath)`。
- 保存源工程（`fguiSaveSourceEdits`）：几何属性全量提交，名称/id 增量提交（对比 `nodeOrig` 快照），并全局替换引用；写回 `FGUI_src/<包名>/<组件>.xml`。
- 侧栏 FGUI 包条目联动：点击/右键「用FGUI编辑器打开」经 `openFguiEditorFromScene` 触发 `enterFguiEditor(binPath)`。

---

## 十五、网络资源抓取模块（page-webgame）

> 代码：`src/pages/webGamePage.js`（页面 UI/入库/收藏夹）、`electron/tools/webGame.js`（`WebGameView` 单例：open/setBounds/close/destroy + `hookWebRequest` 请求拦截 + `classify` 分类 + `downloadResource` 下载 + `probeFile` 本地探测）、`electron/main.js`（web:* IPC）、`electron/preload.js`（10 个 invoke + 4 个事件）、`src/style.css`（`.wg-*` 样式）
> 入口：左侧「🌐 网络资源抓取」根节点（`enterWebGame`）；根下「🕹 最近打开」子节点（点击回填 URL 并打开）；「🔖 网址收藏夹」分类树（点击切到收藏夹面板）。
> 定位：内嵌真实网页（WebContentsView，Electron 43 推荐的 BrowserView 替代方案，独立 `persist:webgame` session 持久化登录态），拦截所有帧（含 4399 登录 iframe）的网络请求，自动分类为 FGUI/Spine/图集/图片/音频/视频/字体/脚本/配置，**下载并按类型入库**（`fgui .bin → addScene(subtype='fgui')`，`spine/image/audio → addItem`，复用现有预览链路）。

### 15.1 整体布局（三区）
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ ① 顶栏 .wg-toolbar                                                         │
│  [URL+状态] ▶打开 ⏹停止 ◀▶⟳ 🔖收藏 </> 🔊静音 🗂隐藏侧栏                  │
│  ── 标签条 #wg-tabs(多标签: 图标+名称+×关闭+＋新开空白标签) ──             │
├──────────────────────────────────────────────────────────────────────────────┤
│ ② 浏览器区 .wg-browser-scroll(flex,可滚动)  ←→  WebContentsView 原生叠加   │
│  ┌──────────────────────────────────────────────────────────────────┐      │
│  │ 网页在此运行；所有网络请求被自动拦截识别为资源                       │      │
│  └──────────────────────────────────────────────────────────────────┘      │
├──────────────────────────────────────────────────────────────────────────────┤
│ ③ 侧栏 .wg-side（上下分割线 #wg-hsplit 可拖动调整 ②/③ 高度比）            │
│  [📡 资源捕获 │ 🔖 网址收藏夹]                                             │
│  ├─ 资源捕获面板：类型筛选 chips + 捕获列表(类型/文件名/URL/大小/状态)     │
│  │  + 输出目录选择 + 仅下载不入库开关 + ⬇下载选中/下载全部/🗑清空 + 进度条 │
│  └─ 网址收藏夹面板：＋收藏网址 / ＋新建目录 + 分类下网址条目(打开/编辑/删除) │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 15.2 顶栏控件（①区）
| id | 名称 | 功能 |
|---|---|---|
| `#wg-url` | URL 输入框 | 输入网址（如 `https://h.api.4399.com/g.php?gameId=100073549`），回车或点「打开」 |
| `#wg-open` | ▶ 打开 | 内嵌浏览器打开 URL 并开始拦截（恢复 `webGameLastUrl`/`webGameHistory`） |
| `#wg-stop` | ⏹ 停止 | 关闭内嵌浏览器（保留捕获记录） |
| `#wg-back`/`#wg-fwd`/`#wg-reload` | ◀ ▶ ⟳ | 网页后退/前进/刷新（`webGoBack`/`webGoForward`/`webReload`） |
| `#wg-devtools` | 🧰 DevTools | 以 `openDevTools({mode:'detach'})` **独立窗口**打开网页 DevTools（Network/Console/Element，便于逆向）；右键按钮可关闭 |
| `#wg-fav` | 🔖 收藏 | 收藏当前浏览网址到收藏夹 |
| `#wg-status` | 状态文本 | 加载状态/标题/URL（`onWebStatus` 事件），位于地址栏 `#wg-url` 尾部（`.wg-url-wrap` 内绝对定位） |
| `#wg-mute` | 🔊 静音 | 网页音频一键静音切换（`web:setAudioMuted` → `WebGameView.setAudioMuted`，静音状态继承到新网页）；播放时显示 🔊，点后变 🔇 |
| `#wg-toggle-side` | 🗂 隐藏侧栏 / 👁 显示侧栏 | 一键隐藏/显示 `.wg-side` 整块（隐藏=折叠高度 `flex:1` 占满浏览器区，再次显示自动重报浏览器视图矩形） |

### 15.3 浏览器区与布局
- 浏览器区：`.wg-browser-scroll` 容器，网页 WebContentsView 原生叠加其上（切页离开时 `webSetBounds({width:0,height:0})` 隐藏，防止遮挡其它页）；超限可滚动。
- 上下分割线 `#wg-hsplit`：拖动调整浏览器区与下方侧栏高度比例（改 `height` 生效，修复 flex-basis 覆盖问题）。

### 15.4 侧栏面板（③区）
两个 tab（`.wg-stab`）：

- **📡 资源捕获**
  - **筛选行** `#wg-filter`（整合于同一行，右侧对齐）：
    - 类型筛选 chips 容器 `#wg-chips`（由 `renderChips` 渲染）：fgui/spine/image/audio/video/font/config/other/script 九类，点击切换显隐，配色 `typeColor`；选择数组持久化 `wg-filter-types`。
    - 计数 `#wg-count`（如「12 条」）+ **全选** `#wg-selall`（仅作用于当前筛选结果）+ 已选计数 `#wg-selcount`。
    - **文件名搜索** `#wg-search`（`type=search`，占位「🔍 搜索文件名...」）：输入实时过滤 `shownRecords()`，与类型 chips / 仅下载不入库叠加；词持久化 `wg-search`。
    - **悬浮预览开关** `#wg-pv-switch`（label「悬浮预览」，持久化 `wg-pv-enabled`）：关时取消悬停自动弹窗、`webPreviewClose` 关闭当前窗。
    - **仅下载不入库** `#wg-onlyurl`：勾选后下载不入库（仍保留文件到输出目录）；**只影响入库不影响列表显示**。
  - **捕获列表** `#wg-list`（`onWebCaptured` 实时推送）：每行结构（v1.9.29 起）：
    - 勾选框（`.wg-rowcheck`，`selected` Set 记录 url）+ 固定 28×28 缩略图插槽 `.wg-thumbwrap`（内 `.wg-thumb` 绝对填满）+ **悬浮线条风格播放按钮** `.wg-playbtn`（内联 SVG 三角，无填充，行悬停显示；点击 `showPreview` 在悬浮预览窗播放，**绕过** `wg-pv-enabled`）。
    - 类型徽标 + 文件名 + URL + 大小 + 下载状态；点击复制 URL。
    - **右键菜单** `ctxActions`/`showMenu`：💾 保存此资源 / 📁 **另存..** `saveAsRec`（目录选择器，默认定位 `webGameSaveDir`，spine 组配套一并另存，不改顶栏输出目录）/ 👁 预览 `showPreview` / 📂 **打开下载目录** `openDownloadDir`（已下载项显示）/ 🔗 复制 URL。
  - 输出目录 `#wg-dir` + 「📁 选择」（持久化 `webGameSaveDir`）+ **「📂 打开目录」** `#wg-open-dir`（跳 `shell:openPath(downloadRoot)`）。
  - 下载：`#wg-dl-sel` 下载选中、`#wg-dl-all` 下载全部（主进程 https 下载，rejectUnauthorized:false + Referer/UA 防盗链 + 可选代理；`onWebProgress` 进度条，`.wg-progress-fill`）；`#wg-clear` 清空捕获。
  - 入库：`downloadResource` 后本地 `probeFile` 精确分类（`.bin` 魔数 FGUII / spine json 特征），fgui `.bin → addScene(subtype='fgui')`、`spine` 自动配对同目录 `.atlas` 填 atlasPath、`image/audio → addItem`，标签 `webgame` + host。

- **🔖 网址收藏夹**
  - 工具栏：＋收藏网址 `#wg-bm-add-url`（默认预填当前网址 `webGetUrl`）/ ＋新建目录 `#wg-bm-add-cat`；显示当前分类名 `#wg-bm-catname`（「全部」或分类名，**无「未分类」概念**，v1.9.9 起收藏必须选分类）。
  - 列表 `#wg-bm-list`：分类下网址条目，每行含「▶ 打开（**新标签** `openUrl`/`openOrSwitch`）/ ✎ 编辑 / 🗑 删除」；右键「📂 移动到...」`moveBookmarkDialog`；内部 tab 点击隐藏浏览器、侧栏收藏夹节点点击保留浏览器（`keepBrowser`）。
  - 数据：`web_bookmark_categories`/`web_bookmarks` 两表；支持嵌套子目录；侧栏「🔖 网址收藏夹」分类树可增删改查并联动本面板（`refreshTree`→`pageEl._webGameTreeRefresher`）。

### 15.5 侧栏与持久化
- 侧栏根「🌐 网络资源抓取」(`__webgame__`)：下含「🕹 最近打开」(`webGameHistory`，最多 8 条，点击回填 URL 打开) 与「🔖 网址收藏夹」(`__webgame_fav__`，可嵌套，默认展开)。根节点右键：打开抓取页 / 新建收藏夹目录 / 清空捕获记录。
- 持久化：`webGameLastUrl` / `webGameSaveDir` / `webGameProxy` / `webGameHistory`（设置项）；捕获记录跨重启保留（`webGetCaptured`）；筛选 chips / 搜索词 / 仅下载不入库 / 悬浮预览开关经 localStorage 持久化（见 **十七、启动与状态持久化**）。

### 15.6 多标签页浏览（v1.9.7+，`#wg-tabs`）
- `WebGameView` 由单 `WebContentsView` 升级为 `tabs: Map<id,{view,url,title}>` 多标签；`syncBounds` 仅活动标签显示在浏览器矩形、其余 `0×0` 隐藏（满足 WebContentsView 叠放约束）。
- 顶栏下标签条 `#wg-tabs`（`.wg-tabs/.wg-tab/.wg-tab-add`）：每标签=图标+名称+×关闭；＋新开空白标签聚焦地址栏（空标签自动隐藏）；点击切换、×关闭（关闭活动标签自动切相邻）；`onWebStatus navigated` 同步地址栏。
- 主进程 `newTab/switchTab/closeTab/getUrl`（main.js `web:newTab/switchTab/closeTab/getUrl` + preload `webNewTab/webSwitchTab/webCloseTab/webGetUrl/onWebTabs`）；侧栏收藏夹/最近历史/收藏夹行打开统一走 `openUrl`→`openOrSwitch`（切换优先，强制新开走 `openUrlNewTab`）。
- 切走抓取页：`_webGameDetach` 经 `webFloatOut()` 把活动标签视图迁入**网页悬浮窗**（见 15.9）；切回经 `_webGameSyncBounds→webSetBounds→floatBack` 收回。

### 15.7 资源归类修正（v1.9.4 / v1.9.10 / v1.9.12）
- `fixRecordTypes()`（按 `(目录, base 名)` 分组）：组内含 `.skel/.atlas(.txt)` → spine 组，组内 `SPINE_SIB_EXT`（`.bin/.skel/.atlas/.atlas.txt/.astc/.png`）全归 `spine`；非 spine 组的 `.bin/.fui` 归 `fgui`；`.sk` 经 `EXT_TYPE` 直接归 `spine`（`[/\.sk(\?|$)/i,'spine']`）。
- spine **预览图**：取同组（同目录同 base）第一个 `.png` 作 `rec.thumb`；组内 `.atlas/.atlas.txt/.png/.astc` 标 `groupOnly`（随主文件整组保存、不单独入库，避免重复 spine 条目）。
- 下载保存路径改为 `{输出目录}/{网站域名}/{URL 相对路径目录}`（去掉原 gameName/type 层）；spine 主文件下载后 `downloadSpineGroup` 把同组配套 `.atlas/.atlas.txt/.png/.skel/.bin/.sk` 一并下载到同一目录。
- 缩略图条件由 `r.type==='image'` 改为 `isImageUrl(r.url)`（spine 组图片仍显示）；入库 `importToLibrary` 对 `groupOnly` 直接跳过。

### 15.8 资源悬浮预览窗（v1.9.0+，独立 BrowserWindow）
> 代码：`electron/tools/webPreviewWindow.js`（单例 BrowserWindow 420×360，可缩放，partition `persist:webgame`）+ `public/preview-window.html` + `electron/previewPreload.js`；渲染端 `src/pages/webGamePage.js` 的 `showPreview`/`schedulePreview`/`pvMouse`。
- **为什么独立窗口**：内嵌浏览器是 WebContentsView 原生视图，永远叠在 DOM 之上（z-index 无法超过），DOM 浮层盖不住；v1.9.0 起改为独立 BrowserWindow（像 DevTools detach），彻底无遮挡/无黑屏/无冻结。
- **交互**：悬停资源行 350ms → 独立预览窗出现在鼠标旁（v1.9.19 起默认**鼠标右下方**，右侧/下方放不下翻转到左/上，clamp 到所在显示器）；移出 280ms 自动隐藏（置顶时除外）；点进窗口即自动置顶常驻（📌），可拖到浏览器区上方任意位置；✕ 关闭/OS 关闭后置顶状态复位。
- 内容按类型渲染：图片 / 音频 / 视频（带 autoplay）/ 字体 @font-face / 文本脚本 / **FGUI/bin**（「在 FGUI 编辑器中打开」）/ **Spine**（「下载并加入资源库」）+ 底部统一「⬇ 保存此资源」`saveSingleRec`。
- **播放按钮** `.wg-playbtn`（v1.9.22 / 1.9.29）：行悬停显示，点击 `showPreview(rec)` **显式触发**（绕过 `wg-pv-enabled` 开关）；右键「👁 预览」用菜单弹出位置。
- 缩略图兜底：`webThumbFetch`→`WebGameView.fetchToDataUrl()`（ses.fetch credentials:'include' + referer，共享 cookie/登录态，4MB 上限，mime 扩展名表 `THUMB_MIME`），`dataset.tried` 防死循环 + `thumbCache`。
- 位置/大小持久化（v1.9.21）：`userData/web-preview-state.json`（resize/move/close 节流保存，恢复上次大小/lastPos）。

### 15.9 网页悬浮窗（v1.9.14+，`floatWin`）
> 代码：`electron/tools/webGame.js` 的 `floatWin`（frameless BrowserWindow + `public/float-window.html` 自绘标题栏 `🌐 标题 + ─ 最小化 + ✕ 关闭`，`-webkit-app-region: drag` 原生拖拽）+ `electron/floatPreload.js`；main.js `web:floatOut/floatBack` + `float:minimize/close`；preload `webFloatOut/webFloatBack`。
- **触发**：切走抓取页（`_webGameDetach`）时把活动标签的 WebContentsView 在主窗口与悬浮窗间迁移（`_moveView`：`removeChildView`/`addChildView`），`floatOut` 迁入悬浮窗并显示、推送标题；切回抓取页 `floatBack` 迁回主窗口并隐藏悬浮窗。
- 标题栏按钮：─ **最小化**→ 在原位置居中缩小为 **64×32 迷你按钮**（仅 ▶ 还原 + ✕ 关闭，v1.9.24 起真正关闭/最小化行为）；✕ **关闭**→ 真正关闭（视图迁回主窗口 + 销毁悬浮窗，退出悬浮模式；v1.9.24 起修复「最小化到左上角小窗」）。
- `pauseMedia()`：切离/关闭时向活动标签注入 JS 暂停全部 `video/audio`（参考 Chrome 后台标签页），切走页面即停止网页媒体播放（v1.9.25）。
- `syncBounds` 悬浮时忽略主窗口布局；窗口 resize 同步视图 bounds；tab 记录所在窗口，closeTab/close/destroy 兼容悬浮窗；`openOrSwitch` 已开相同 URL 切换、否则新开（v1.9.26 修复左上角小窗）。
- 位置/大小持久化（v1.9.21）：`userData/webgame-float-state.json`（300ms 节流；显示器变更回退默认左上角）；v1.9.23 起最小化收为 **64×36 迷你按钮**（alwaysOnTop，JS pointer 拖拽，点击还原）。

### 15.10 侧栏收藏夹交互修复（v1.9.26 / v1.9.27）
- `enterWebGame()`（v1.9.27）：**已在抓取页时跳过 `clearOverlays()`**（仅首次进入才清状态/触发 detach），避免重复 detach→floatOut 把网页视图迁出主窗口导致浏览器区黑屏 + 弹悬浮窗。
- `setPanel(panel, keepBrowserInPanel)`（v1.9.27）：`keepBrowser` 时收藏夹面板下**保留浏览器视图**（走 `_webGameSyncBounds` 而非 `0×0`）；左树分类/根节点/右键「打开收藏夹」点击传入 `{ keepBrowser: true }`，有网页打开时点击收藏夹目录**内容保持不黑屏**。
- `openOrSwitch(url)`（v1.9.26）：`normUrl` 规范化比较（忽略尾斜杠/hash），已打开相同 URL 标签页→切换，否则新开；避免重复开标签 + 修复左上角小窗。

---

## 十六、API 管理模块（page-api）

> 代码：`src/pages/apiPage.js`（页面/双标签页/三级 CRUD/接口测试面板）、`electron/tools/apiTest.js`（接口测试代理）、`electron/db.js`（三表）、`src/state.js`（三级状态 + CRUD）、`public/api-doc.html`（内嵌 API 参考文档）、`src/style.css`（`.page-api` / `.apm-*` 样式）
> 入口：左侧「🛠️ 开发工具箱」根 `__devtools__` →「📖 API 管理」叶子 `__devtool:api`（`enterApiDoc`）；面包屑「主页 / 开发工具箱 / API 管理」；主区多标签「API 管理」。
> 定位：开发辅助模块，提供 **API 项目管理 + 数据字典 + 接口测试**（双标签页「🗂 项目管理 / 📖 API 文档」）。文档 tab 以 iframe 隔离内嵌 `api-doc.html`（Stripe 风格 API 参考文档，零外部依赖，自身样式/脚本/主题/语言切换 100% 保真，与主应用互不污染）。

### 16.1 整体布局（双标签页）
```
┌──────────────────────────────────────────────────────────────────────────────┐
│ .apm-tabs: [🗂 项目管理] [📖 API 文档]                                        │
├──────────────────────────────────────┬───────────────────────────────────────┤
│ .apm-project(#apm-project)            │ .apm-doc(#apm-doc, iframe)            │
│  .apm-left(#apm-left):               │  <iframe class="api-doc-frame"        │
│    分类/项目树(#apm-cat-tree) + ＋分类 │   src="./api-doc.html">               │
│  .apm-main(#apm-main):               │                                       │
│    总览统计 / 分类视图(项目卡片) /      │                                       │
│    项目视图(Base URL+数据字典+详情表单) │                                       │
└──────────────────────────────────────┴───────────────────────────────────────┘
```

### 16.2 项目管理（🗂 标签页）
- **三级数据模型**（SQLite，经 `db.js` 三表 + `state.js` 数组）：`api_categories`（分类树，可嵌套）→ `api_projects`（项目，挂分类下）→ `api_endpoints`（数据字典接口）。
- **左栏** `#apm-left`：分类树 `#apm-cat-tree`（可嵌套/折叠/右键菜单）+ 未分类节点 `__uncat__`；顶部「＋ 分类」`#apm-add-cat` 新建顶级分类。
- **主区** `#apm-main`：
  - 总览统计（分类/项目/接口数）。
  - 分类视图：该分类下**项目卡片**（点击进入项目）。
  - 项目视图：Base URL + **数据字典列表** + **接口详情表单**（名称/方法/路径/说明/请求参数表/请求头表/请求体/响应示例，保存按钮）。
- **接口测试面板**：URL（默认 baseUrl+path）/ 方法 / 请求头（"Name: Value" 行）/ 请求体 / 超时 / 代理；发送后经主进程 `apiTest.js` 代发（Node http/https，rejectUnauthorized:false + 可选代理 + 重定向≤5 + 响应 2MB 截断），展示状态码/耗时/大小/响应头/格式化响应体/复制。
- 关键函数：`renderApiPage`（初始化容器，复用实例保留滚动/语言/主题）、`renderCatTree`/`renderMain`、`addApiCategory`/`addApiProject`/`addApiEndpoint` 等 CRUD（删分类子分类提升 + 项目移未分类；删项目级联删接口）。

### 16.3 API 文档（📖 标签页）
- iframe `.api-doc-frame`（`src="./api-doc.html"`，`allow="clipboard-read; clipboard-write"`），原 `E:\MyProject\api_page\index.html` 移植到 `public/api-doc.html`（vite 随 dist 输出，本地 HTTP 直接可访问），懒加载 + 复用实例。

---

## 十七、启动优化与状态持久化（v1.9.16 ~ v1.9.23）

### 17.1 启动提速（v1.9.16）
- **PixiJS 动态导入**：新建 `src/pixiLazy.js`（`getPixi`/`pixiRef`），移除全部 7 处静态 `import * as PIXI`，首次预览/缩略图/FGUI 编辑时才加载 918KB pixi（首屏只加载 438KB index）；`@pixi/spine-pixi` 同步动态导入；`window.PIXI` 由 getPixi 首次加载时设置（DragonBones UMD 兼容）。
- **localStorage 延迟**：`src/ui.js` 侧栏隐藏状态改 `requestIdleCallback`/空闲读取（原首次访问约 6.5s 阻塞首屏）；移除 initUI 末尾重复渲染。
- **预览渲染器延迟**：`src/main.js` `preview.init`（PIXI.Application/WebGL）延后到首屏渲染后后台初始化，首次预览时 `ensure`。
- 效果：首屏渲染 gap ~4.9s → ~4ms。

### 17.2 启动骨架屏（v1.9.17）
- `index.html` body 内 `#splash`（纯内联 CSS：🎮 图标 + 「游戏资源管理器」标题 + 加载动画，CSP 允许 unsafe-inline），渲染进程首帧即可绘制。
- `electron/main.js`：窗口 `show:false` + `ready-to-show` 后再显示（首帧=骨架屏就绪），5s 兜底强制显示；`src/main.js` 首屏渲染完成后移除 `#splash`。全程无黑屏。

### 17.3 localStorage 持久化根因修复（v1.9.23）
- 根因：`electron/server.js` `server.listen(0)` **端口随机** → 渲染端 origin（含端口）每次启动不同 → localStorage 按 origin 隔离，全部持久化状态（悬浮预览/仅下载不入库/类型筛选/搜索词/侧栏/音频模式等）重启后丢失。
- 修复：**固定端口 13456**（EADDRINUSE 时 +1 重试，最多 30 次）→ origin 稳定，localStorage 持久化全部恢复生效。

### 17.4 状态持久化清单
- **网络资源抓取**（`localStorage`，`src/pages/webGamePage.js`）：`wg-pv-enabled`（悬浮预览开关）、`wg-only-url`（仅下载不入库）、`wg-filter-types`（类型筛选 chips 数组 JSON）、`wg-search`（文件名搜索词）。
- **音频播放器**（`localStorage`，`src/viewers/audioViewer.js`）：`audio-mode`（播放模式）/`audio-rate`（倍速）/`audio-volume`（音量），init 恢复并同步控件。
- **悬浮窗**（`userData/*.json`，主进程节流保存）：网页悬浮窗 `webgame-float-state.json`（位置+大小）、资源悬浮预览窗 `web-preview-state.json`（大小+lastPos），重启恢复。
- **既有持久化**：输出目录/最近网址/历史/代理（`settings.webGameSaveDir/LastUrl/History/Proxy`）、目录列表页视图/排序、场景页分割尺寸、工具箱输入历史、侧栏 `expandedCats`/`sidebarHidden`。

### 17.5 退出彻底化（v1.9.18）
- `electron/main.js` 主窗口 `win.on('closed')` 主动清理附属窗口（`webPreviewWindow.close()` + `webGame.destroy()` 内部销毁 floatWin）并 `app.quit()`，不再依赖 `window-all-closed`；所有 IPC handler 先于 `createWindow` 注册（修复双重 createWindow + handler 顺序错误）。

---

*文档版本：v1.9.29 · 2026-08-11*
